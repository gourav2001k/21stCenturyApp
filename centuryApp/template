import React from 'react';
import {View, Text, StyleSheet, Button,Dimensions} from 'react-native';

const height = Dimensions.get('screen').height;
const width = Dimensions.get('screen').width;

const MealCard = props => {
  return (
    <View style={styles.container}>
      <Text>This is MealCard Screen!!!</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default MealCard;

App Description
Note: Currently in Testing  Phase.
We are a bakery in your own town. The users of this region can now order our food through online medium as well. Just order from this app and leave the rest to us. Be it any occasion from Birthdays, Anniversaries or any other celebration  we have got you covered.
Keep Celebrating